# WebSocket upgrade map — must be included at the http {} level,
# NOT inside a server {} block. Include this in the main nginx.conf:
#   include /etc/nginx/conf.d/http-maps.conf;

map $http_upgrade $connection_upgrade {
    default upgrade;
    ''      close;
}
