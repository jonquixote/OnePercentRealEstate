// Moved to @oper/observability so apps/two shares one implementation rather
// than a copy that drifts. Re-exported here so existing imports keep working.
export * from '@oper/observability/perf-track';
