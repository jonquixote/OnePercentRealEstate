#!/usr/bin/expect -f
set timeout 60
spawn ssh -o StrictHostKeyChecking=no -o PreferredAuthentications=password -o PubkeyAuthentication=no root@157.245.184.89
expect "password:"
send "appo0-buXbym-cijzy\r"
expect "root@"

send "docker ps | grep infrastructure_app\r"
expect "root@"
send "docker ps | grep scraper\r"
expect "root@"

# Check if build process is still running
send "ps aux | grep 'docker compose' | grep -v grep\r"
expect "root@"

send "exit\r"
expect eof
