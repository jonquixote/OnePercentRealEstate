# age recipient (PUBLIC key) for the encrypted secrets bundle in backup-postgres.sh.
# Safe to commit: this only ENCRYPTS. The matching private key is held off-box by
# the operator and is the only thing that can decrypt the config-*.tar.age files.
#
# ROTATED 2026-08-01: the first keypair's PRIVATE key was pasted into a chat
# transcript, which — combined with the R2 credentials also pasted there — would
# have allowed decryption of the secrets bundle. Rotated rather than kept.
age1tfl4n3t82kpm0du0vk0ymrch0zmm7fr6glggf8al29uz2nzwlpzs7dqlvz
