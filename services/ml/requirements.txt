# Minimal runtime for the rent-prediction FastAPI shim. The actual
# triangulation math lives in services/rent_estimator_v2.py which we
# import; this image only needs the HTTP frame, the DB driver, and the
# eval/drift CLI dependencies.
fastapi==0.115.5
uvicorn[standard]==0.32.0
psycopg2-binary==2.9.10
python-dotenv==1.0.1
# pandas + numpy are pulled in for eval/drift CLIs even if the /predict
# endpoint never imports them.
pandas==2.2.3
numpy==1.26.4
# Wave 2: rent model v1 (training + quantile inference). Needs libgomp1 at
# runtime — provided transitively by build-essential in the Dockerfile.
lightgbm==4.5.0
